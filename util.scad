/******************************************************************************

        Util

/*****************************************************************************/
function get(data, key) = data[search([key], data)[0]][1];
function add(v, i = 0, r = 0) = i < len(v) ? add(v, i + 1, r + v[i]) : r;